# 2026-07-28

## 项目背景
今天的目标是把 Mia's Cruises 项目里的技术层补出来，让 Data Basis 旁边多一个 IT Data Flow 子页，并把平台记忆结构升级成全平台共用的 short / medium / long-term memory。

## 当天目标
- 给 Data Basis 增加技术侧入口
- 建立可复用的 memory architecture
- 让 Mia 能读到新的 flow / memory 上下文
- 把更新同步到日志、文档和 seed

## 行动总结
- 新增 `IT Data Flow` 页面，展示上传、验证、清洗、建模、服务和反馈链路。
- 把平台记忆拆成 short-term、medium-term、long-term 三层，并作为全平台共用能力。
- 更新 Mia 的建议问题、local knowledge base、dashboard context 和后端 project memories。
- 补齐 update log、README、index，以及 Data Basis 的技术跳转。
- 后续把 IT Data Flow 收束为 batch-first 技术评审工作台：首屏只保留 source batch 上传、六阶段 lifecycle 和默认收起的 Mia intake brief；lineage、data contract、quality review、platform architecture 改为按需展开。
- 移除了会分散评审注意力的 Flow map、Processing monitor、常驻右侧 AI 总结和指标卡堆叠。移动端导航同步改为紧凑横向滚动，Mia 入口在收起态收成图标按钮，避免上传入口被菜单或浮层遮挡。

## 实现内容
- IT Data Flow 页支持 txt / pdf / doc / docx 上传、版本监控、可靠性检查、人工反馈和 AI 摘要。
- 显示 source systems、governance checks、visible Excel data types 和 shared memory layers。
- Data Basis 维持业务口径，但增加到 IT Data Flow 的明确跳转。
- 后端新增平台记忆 seeds，供 Mia 和未来检索复用。
- 上传后的前端状态会显示文件类型、总大小、"Ready for validation" 状态和 lifecycle 进度；Mia 的 brief 根据当前 batch 生成预览。当前上传是前端工作台交互，实际文件持久化和异步解析仍是下一阶段集成工作。

## 测试数据与验证
- 已跑 `node work/test_it_data_flow_state.mjs`、`node work/test_location_progress_css.mjs`、`node work/test_language_detection.mjs` 和 `vite build --config vite.config.mjs`。
- 在浏览器完成了 desktop 和 390px mobile smoke check：上传后的 batch metadata / lifecycle、Mia brief、四个 disclosure、人工反馈提交均可工作；默认首屏没有 Flow map 或 Processing monitor，且不存在横向溢出。
- 移动端检查后补充了紧凑导航、两列 lifecycle 和收起态 Mia launcher 的样式，避免导航和浮动入口遮挡上传任务。

## 后续注意点
- 后面最好把 `data/seed.json` 重新导出一次。
- 将真实上传接到后端时，需要补充文件持久化、parser job、版本号、quality gate 和发布审计记录；页面目前清楚地区分了这层未来集成边界。
- 如果后续还要扩 survey 或 passenger-profile 切片，可以继续沿用这套 memory architecture。
